/*
 * Mandatory:
 *
 * vars.volume.name
 * vars.volume.size
 *
 */

/*
 * 
 */
bb-volumes-yaml(parent, vars) ::= <<
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: <vars.volume.name>
  namespace: robobeerun-com-elastic
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: <vars.volume.size>

>>
